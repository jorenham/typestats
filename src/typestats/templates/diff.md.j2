{% from "_macros.j2" import metric_cell %}
---
hide:
  - navigation
  - toc
---

# {{ package }} Version History

{% if chart %}
!!! info "Coverage timeline"

    ``` mermaid
    ---
    config:
        xyChart:
            width: 1600
            height: 320
            xAxis:
                labelPadding: 16
        themeVariables:
            xyChart:
                plotColorPalette: "{{ chart.palette }}"
                backgroundColor: "transparent"
    {% for key in chart.theme_colors %}
                {{ key }}: "currentColor"
    {% endfor %}
    ---
    xychart-beta
        x-axis [{{ chart.labels | map('tojson') | join(', ') }}]
        y-axis "Coverage (%)" 0 --> 100
        line [{{ chart.cov | join(', ') }}]
        line [{{ chart.strict_cov | join(', ') }}]
    ```

    <div class="chart-legend" markdown>
      <span class="chart-legend-item">
        <span class="chart-legend-swatch chart-legend-swatch--cov"></span> Coverage
      </span>
      <span class="chart-legend-item">
        <span class="chart-legend-swatch chart-legend-swatch--strict"></span> Coverage (strict)
      </span>
    </div>
{% endif %}

<table>
  <thead>
    <tr>
      <th>Version</th>
      <th style="text-align: right">
        <abbr title="Release date on PyPI">Released</abbr>
      </th>
      <th style="text-align: right">
        <abbr title="Percentage of annotated symbols">Coverage</abbr>
      </th>
      <th style="text-align: right">
        <abbr title="Percentage of annotated symbols, excluding Any">Coverage (strict)</abbr>
      </th>
      <th style="text-align: right">
        <abbr title="Number of public annotatable slots: each function parameter, return type, and variable counts as one">Symbols</abbr>
      </th>
      <th style="text-align: right">
        <abbr title="Slots without a type annotation">Unannotated</abbr>
      </th>
      <th style="text-align: right">
        <abbr title="Number of type-checker ignore comments">Ignores</abbr>
      </th>
    </tr>
  </thead>
  <tbody>
    {% for row in rows %}
      <tr>
        {% if loop.first %}
          <td><a href="../">{{ row.version }}</a></td>
        {% else %}
          <td>{{ row.version }}</td>
        {% endif %}
        <td style="text-align: right">{{ row.release_date }}</td>
        {{ metric_cell(row.coverage) }}
        {{ metric_cell(row.coverage_strict) }}
        {{ metric_cell(row.symbols) }}
        {{ metric_cell(row.unannotated) }}
        {{ metric_cell(row.ignores) }}
      </tr>
    {% endfor %}
  </tbody>
</table>
